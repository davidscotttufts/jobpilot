{
  "name": "jobpilot",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "src/web"
  ],
  "scripts": {
    "sync-skills": "bun scripts/sync-skills.ts",
    "predev": "bun run sync-skills",
    "dev": "concurrently -k -n terminal,web -c cyan,green \"dotnet run --project src/JobPilot.Terminal\" \"bun --cwd=src/web run dev\"",
    "db:setup": "bun --cwd=src/web run db:generate && bun --cwd=src/web run db:migrate:apply && bun --cwd=src/web run db:seed",
    "prebuild:terminal": "bun run sync-skills",
    "build:terminal": "dotnet publish src/JobPilot.Terminal -c Release -o dist/terminal",
    "build:web": "bun --cwd=src/web run build"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "@types/bun": "^1.3.14",
    "concurrently": "^9.1.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
