generator client {
  provider        = "prisma-client"
  output          = "../../src/generated/prisma"
  previewFeatures = ["postgresqlExtensions", "relationJoins"]
}

datasource db {
  provider   = "postgresql"
  // pg_trgm backs the job_listings trigram search indexes.
  extensions = [pg_trgm]
}
